# Stage 3 UMLS Mapping - Cross-Encoder Training Dependencies
# Install after PyTorch: pip install -r requirements-training.txt

# Core Training Libraries
# NOTE: Use compatible versions with gfmrag package (requires transformers>=4.46.1)
transformers>=4.46.1      # HuggingFace transformers for BERT models
datasets>=2.14.0          # HuggingFace datasets for data loading
tensorboard>=2.14.0       # Training visualization and monitoring
scikit-learn>=1.3.0       # Metrics (precision, recall, F1, ROC-AUC)
huggingface-hub>=0.20.0   # Required by accelerate and sentence-transformers

# Visualization & Analysis
matplotlib>=3.7.0         # Plotting calibration curves, PR curves
seaborn>=0.12.0          # Statistical visualization
pandas>=1.5.0            # Data analysis and manipulation

# Model Training Utilities
accelerate>=0.20.0       # Multi-GPU training support
tqdm>=4.65.0             # Progress bars
safetensors>=0.3.0       # Safe model serialization

# Evaluation & Metrics
scipy>=1.10.0            # Statistical functions for calibration
numpy>=1.24.0,<2.0.0     # Numerical operations (faiss-gpu requires <2.0)

# Data Processing
tokenizers>=0.14.0       # Fast tokenization
sentencepiece>=0.1.99    # Subword tokenization (for some models)

# Optional but Recommended
ipython>=8.12.0          # Interactive Python shell
jupyter>=1.0.0           # Jupyter notebooks for analysis

# Additional dependencies for compatibility
faiss-cpu                # Required by ragatouille (if not using GPU version)
